# ReflectionParser

## 概述
ReflectionParser 是一个基于 .NET 的反射代码生成工具，用于解析 C++ 头文件中的反射宏并生成对应的 `.generated.h` 文件。

## 开发进度
- 目标框架已从 `net10.0` 降级为 `net9.0`，以支持未安装 VS2026 的远程协作机器。
- 当前 SDK 版本：.NET 9.0.316
- 项目可正常编译并生成 `ReflectionParser.dll`。

## 环境要求
- .NET 9.0 SDK 或更高版本
