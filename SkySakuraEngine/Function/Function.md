# 功能层内容目录

功能层的主要目的是在核心层与平台层的基础上构建出更完整的功能服务。目前考虑了如下内容。

* `./EngineCore` ：引擎核心框架，包括引擎的启动引导函数与结束引导，以及在运行时整合构建引擎内的其他功能。
* `./GameWorld`：主要包括游戏世界的对象基类，并含有组件类的基类。
* `./OpenGLExtension`：主要是考虑在 OpenGL 的基础上构建更多完整的功能服务。也可以放在平台层（？）